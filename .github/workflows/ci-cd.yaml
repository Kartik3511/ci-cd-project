name: CI/CD Pipeline

on:
  push:
    branches: [ main ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    env:
      IMAGE_NAME: ci-cd-demo
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install dependencies (including dev)
        run: npm --prefix app ci

      - name: Run tests
        run: npm --prefix app test

      - name: Log in to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v4
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/${{ env.IMAGE_NAME }}:${{ github.sha }},${{ secrets.DOCKERHUB_USERNAME }}/${{ env.IMAGE_NAME }}:latest

      # Optional deploy (only if you set KUBE_CONFIG_DATA secret to base64 kubeconfig)
      # - name: Set up kubectl
      #   uses: azure/setup-kubectl@v3
      #   with:
      #     version: 'v1.27.0'
      #
      # - name: Configure kubeconfig
      #   run: |
      #     echo "${{ secrets.KUBE_CONFIG_DATA }}" | base64 --decode > kubeconfig
      #     mkdir -p $HOME/.kube
      #     mv kubeconfig $HOME/.kube/config
      #
      # - name: Deploy to Kubernetes
      #   run: |
      #     kubectl set image deployment/ci-cd-demo ci-cd-demo=${{ secrets.DOCKERHUB_USERNAME }}/${{ env.IMAGE_NAME }}:latest --record
      #     kubectl rollout status deployment/ci-cd-demo --timeout=120s
